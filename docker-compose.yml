services:
  warehouse:
    build: .
    ports:
      - "8080:8080"
    restart: unless-stopped
    stop_grace_period: 15s
    # Pass launch arguments as command, e.g.:
    #   command: ["travel_speed:=3.0", "min_battery_1:=40.0"]
